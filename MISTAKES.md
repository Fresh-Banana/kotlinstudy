# 실수 기록

작업 중 발생한 실수나 사용자가 지적한 잘못된 접근을 기록하는 파일.
새 항목을 추가할 때는 날짜와 함께, 무엇을 잘못했고 앞으로 어떻게 해야 하는지 적는다.

## 기록

### 2026-08-06 — 파일 확인 없이 "dotenv 미설정"이라고 단정
- 무엇을 잘못했나: 구글 OAuth 환경변수가 로드되는지 물어봤을 때, `build.gradle`을 확인하지 않고 "OS 환경변수만 보고 .env는 자동으로 안 읽는다"고 단정해서 답변함. 실제로는 `spring-dotenv` 라이브러리가 이미 의존성에 추가되어 있어서 `.env`가 정상적으로 로드되는 상태였음.
- 앞으로 어떻게: 설정/의존성 관련 질문에 답하기 전에 `build.gradle`(or `build.gradle.kts`) 등 실제 설정 파일을 먼저 확인하고 답한다. 추측으로 단정하지 않는다.

### 2026-08-06 — 셸 명령으로 `.env` 시크릿 값을 그대로 출력
- 무엇을 잘못했나: `.env` 파일 포맷을 확인한다며 `cat .env`를 그대로 실행해서 구글 OAuth client-id/secret 실제 값이 대화 로그에 그대로 노출됨.
- 앞으로 어떻게: 시크릿이 들어있을 만한 파일(`.env` 등)을 확인할 때는 값을 마스킹하거나 키 이름만 추출해서 보여준다 (예: `grep -v '^#' .env | sed 's/=.*/=<hidden>/'`). 값 자체를 확인해야 할 특별한 이유가 없으면 절대 그대로 출력하지 않는다.
