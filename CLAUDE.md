# kotlinchating

Kotlin + Spring Boot 기반 채팅 서버. JPA(PostgreSQL)로 데이터 학습 겸 CRUD를 구현하고, 이후 JWT 인증과 WebSocket/STOMP 실시간 채팅을 얹는 것이 목표.

## 스택
- Kotlin, Spring Boot 4.1.0 (Gradle)
- DB: PostgreSQL, Spring Data JPA (`spring.jpa.hibernate.ddl-auto=update`)
- 인증: Spring Security + JJWT
- 세션: spring-session-jdbc
- 실시간: spring-boot-starter-websocket (STOMP)

## 명령어
- 빌드/테스트: `./gradlew build`
- 테스트만: `./gradlew test`
- 실행: `./gradlew bootRun`
- PostgreSQL이 로컬 5432 포트에 떠 있어야 함 (`application.properties` 참고, db명 `kotlinchating`)

## 코드 컨벤션
- 엔티티는 `crud/entity`, 레포지토리는 `crud/repository` 등 기능별 하위 패키지로 나눈다.
- 엔티티는 주 생성자 파라미터에 `@field:Column` 등 JPA 애노테이션을 직접 붙이는 패턴을 따른다. 더미 기본값(`= ""`, `= Sex.MALE` 등)이나 별도의 no-arg 생성자는 두지 않는다 — `kotlin-jpa` 플러그인(`build.gradle`)이 `@Entity` 클래스에 no-arg 생성자를 자동 생성해준다.
- 엔티티에는 `@Id`를 반드시 명시한다.
- 레포지토리는 Spring Data JPA의 `JpaRepository<Entity, IdType>` 인터페이스로 작성한다 (구현체 직접 작성 X).

## 작업 방식 (중요)
- **코드는 직접 파일에 쓰지 말고 채팅에 코드 블록으로 보여줄 것.** 사용자가 직접 타이핑하고 싶어함.
- 파일 생성/수정은 사용자가 명시적으로 "만들어줘/수정해줘/파일에 써줘"라고 요청했을 때만 수행한다.
- 아직 초기 학습 단계 프로젝트이므로 과도한 추상화나 기능 추가 없이, 요청한 범위만 다룬다.

## 코딩 원칙 (Karpathy 가이드라인 기반)

사소한 수정(오타, 한 줄짜리)까지 아래 원칙을 다 지킬 필요는 없음. 규모 있는 작업에 적용.

### 1. 먼저 생각하기
- 불확실하면 추측해서 넘어가지 말고 물어본다.
- 해석이 여러 갈래로 갈리면 하나를 임의로 골라 진행하지 말고 옵션을 제시한다.
- 더 단순한 방법이 있으면 그걸 먼저 말한다.

### 2. 단순하게
- 요청한 것 이상의 기능/추상화/설정 옵션을 추가하지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 만들지 않는다.
- "시니어 개발자가 보기에 과하게 복잡한가?"를 기준으로 판단한다.

### 3. 필요한 부분만 수정
- 기존 코드/주석/포맷을 요청받지 않았는데 "개선"하지 않는다.
- 기존 스타일을 그대로 따른다 (예: `User.kt`의 data class + 빈 생성자 패턴).
- 내가 만든 변경으로 인해 안 쓰게 된 import/변수만 정리한다. 원래 있던 죽은 코드는 요청 없이 지우지 않는다.

### 4. 목표와 검증 기준을 명확히
- "버그 고쳐줘" → 재현 방법을 먼저 확인하고, 고친 뒤 어떻게 확인할지까지 제시.
- 여러 단계가 필요한 작업이면 시작 전에 간단한 단계별 계획을 먼저 보여준다.

### 5. 보안은 학습 단계와 무관하게 실제 서비스 배포 수준으로
- "아직 학습 단계니까 나중에 고치자"는 이유로 보안 이슈를 넘어가지 않는다 (예: 응답에 비밀번호 필드 노출, 민감정보 로깅 등).
- 보안 관련 트레이드오프를 발견하면 바로 언급하고, 실제 배포하는 서비스라고 가정하고 수정 방향을 제시한다.

## 실수 기록
- 실행될 때마다 `MISTAKES.md`를 확인하고, 거기 적힌 실수를 반복하지 않는다.
- 작업 중 실수를 했거나 사용자가 잘못을 지적하면, 그 내용을 `MISTAKES.md`에 추가로 기록한다.

## 하지 말 것
- `jwt.secret` 등 `application.properties`의 비밀값을 하드코딩된 예제 밖에서 실제 값처럼 다루지 않는다 (현재 값은 placeholder).
- 사용자가 요청하지 않은 리팩토링/파일 생성을 먼저 하지 않는다.
